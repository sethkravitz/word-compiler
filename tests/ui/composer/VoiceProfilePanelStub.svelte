<script lang="ts">
// Lightweight stub for VoiceProfilePanel used in SetupPanel tests so we don't
// need to mock the full voice API surface. The real component triggers API
// calls in $effect on mount; we render a sentinel instead.
</script>

<div data-testid="voice-profile-panel-stub">voice profile panel stub</div>